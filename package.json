{
  "name": "finanza",
  "version": "4.3.1",
  "description": "App de controle financeiro pessoal",
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "ios": "npx cap open ios",
    "build:apk": "npx cap sync android && cd android && gradlew.bat assembleDebug",
    "check": "node --check server/index.js && node --check server/permissions.js && node --check server/transactionParser.js && node --check server/backupSchema.js && node --check server/backupMigrations.js && node --check frontend/app.js && node --check frontend/dashboard.js && node --check frontend/bootstrap.js && node --check frontend/transactions.js && node --check frontend/future.js",
    "test": "node --test tests/*.test.js",
    "migrate": "node migrate-local-to-db.js",
    "migrate:fisiosaas": "python tools/fisiosaas_to_finanza.py"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/local-notifications": "^8.0.2",
    "@capacitor/push-notifications": "^8.0.3",
    "@capacitor/splash-screen": "^8.0.0",
    "@capacitor/status-bar": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "pg": "^8.11.3"
  }
}
