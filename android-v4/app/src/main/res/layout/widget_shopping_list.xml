<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="vertical"
    android:gravity="center_vertical"
    android:padding="12dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/dm_sans_700"
        android:includeFontPadding="false"
        android:text="@string/widget_shopping_title"
        android:textColor="#F0F3FF"
        android:textSize="15sp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:fontFamily="@font/dm_sans_500"
        android:includeFontPadding="false"
        android:text="@string/widget_shopping_subtitle"
        android:textColor="#B8F0F3FF"
        android:textSize="11sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="12dp"
        android:baselineAligned="false"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/widget_open_shopping"
            style="@style/WidgetQuickAction"
            android:text="@string/widget_action_open_shopping" />

        <TextView
            android:id="@+id/widget_add_shopping_item"
            style="@style/WidgetQuickAction"
            android:text="@string/widget_action_add_item" />
    </LinearLayout>
</LinearLayout>
