# Avisos pendentes

- Rotacionar as senhas/connection strings do Neon e do Render Postgres que foram expostas durante a recuperacao.
- Remover o usuario de teste `gustavo` do Neon quando o login `jefferson` estiver validado em todos os dispositivos.
- Decidir se os scripts `tools/neon-doctor.js` e `tools/restore-backup-to-db.js` ficam como utilitarios permanentes ou se devem virar procedimento documentado separado.
- Revisar o plano do banco: Render Postgres gratuito expirou; manter Neon como fonte principal ou contratar banco persistente.
